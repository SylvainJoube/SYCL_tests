/// find_or_create_graph_points(graph_list, name) : instance of graph_points
/// with newly_created set to true if it was not found and therfore created

find_or_create_graph_points_ext(argument0, argument1, -1);
